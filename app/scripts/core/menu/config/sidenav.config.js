'use strict';

define(function(require){
    var config = {};
    config.outerTpl  = require('jst!../templates/foundation/sidenav/outerTpl.ejs');
    config.tagName = 'div';
    config.className = '';


    return config;
});
